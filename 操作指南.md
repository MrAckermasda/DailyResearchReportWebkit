# 学习科研日推网页操作指南

## 1. 项目里现在有什么

这套工具现在包含 4 部分：

- `学习科研日推网页`
  - 首页、历史归档、周报页、知识树页
- `日推数据构建`
  - 把 Markdown 日推整理成网页可读的数据
- `每周周报初稿`
  - 每周生成 Markdown 周报
- `知识树`
  - 生成 `想学 / 在学 / 已学` 的可视化数据

## 2. 常用文件

- `start_dashboard.ps1`
  - 启动网页前端
- `generate_research_digest.ps1`
  - 生成当日日推 Markdown
- `build_dashboard_data.ps1`
  - 重建网页首页和归档数据
- `generate_weekly_report.ps1`
  - 生成周报初稿
- `build_knowledge_tree.ps1`
  - 重建知识树数据
- `install_daily_push_task.ps1`
  - 安装每日日推计划任务
- `install_weekly_report_task.ps1`
  - 安装每周周报计划任务
- `research_digest_config.psd1`
  - 配置文件

## 3. 启动网页

在项目目录运行：

```powershell
pwsh -File .\start_dashboard.ps1
```

然后浏览器打开：

```text
http://127.0.0.1:4173/
```

如果你想要一个更稳定、可双击的入口，直接双击：

```text
打开学习科研日推网页.cmd
```

这个入口会自动执行：

- 重建网页数据
- 重建知识树
- 刷新周报索引
- 关闭旧的网页服务
- 启动新的本地网页服务
- 自动打开浏览器

## 4. 如果网页打不开

先检查是不是旧进程占着端口，或者根本没启动。

关闭旧网页服务：

```powershell
Get-CimInstance Win32_Process |
Where-Object { $_.Name -eq 'node.exe' -and $_.CommandLine -match 'web\\server\.mjs' } |
ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
```

然后重新启动：

```powershell
pwsh -File .\start_dashboard.ps1
```

如果只想直接启动网页服务，也可以：

```powershell
node .\web\server.mjs
```

但更推荐用 `start_dashboard.ps1`，因为它会先重建需要的数据。

## 5. 日常使用顺序

### 5.1 更新当日日推

```powershell
pwsh -File .\generate_research_digest.ps1
```

这个脚本会：

- 生成当日日推 Markdown
- 顺带刷新网页首页与归档数据

### 5.2 重建网页数据

如果你只改了历史文件、DDL 或网页数据，可以单独运行：

```powershell
pwsh -File .\build_dashboard_data.ps1
```

### 5.3 重建知识树

```powershell
pwsh -File .\build_knowledge_tree.ps1
```

### 5.4 生成周报初稿

```powershell
pwsh -File .\generate_weekly_report.ps1
```

生成结果在：

```text
.\weekly-reports\
```

当前周报主输出为：

- 新生成的 `.docx` 周报文件

同时脚本还会保留一个同名 `.md` 预览文件，供网页周报页继续读取。

## 6. 安装计划任务

### 6.1 每日日推

```powershell
pwsh -File .\install_daily_push_task.ps1
```

### 6.2 每周周报

```powershell
pwsh -File .\install_weekly_report_task.ps1
```

每周周报时间来自：

```text
research_digest_config.psd1
```

当前默认是：

```text
Sunday 20:00
```

## 7. 网页各页面说明

### 首页

- 看今日截止
- 看逾期任务
- 看最近归档入口

### 归档

- 按日期切换历史日推
- 浏览器内直接阅读 Markdown 内容

### 周报

- 查看 `weekly-reports` 下生成的周报初稿

### 知识树

- 看 `想学 / 在学 / 已学`
- 可对已有节点改状态、改备注
- 保存后会写入手工覆盖并重建知识树

## 8. 重要目录

- `data/dashboard/`
  - 首页和归档数据
- `data/dashboard/digests/`
  - 每篇日推对应的 JSON
- `weekly-reports/`
  - 周报初稿和周报索引
- `knowledge-tree/`
  - 自动知识树、手工覆盖、合并结果
- `daily-digests/`
  - 每天生成的学习与科研日推 Markdown
- `web/`
  - 网页前端和本地服务
- `tests/`
  - PowerShell 和 Node 测试

## 9. 配置文件说明

配置文件：

```text
research_digest_config.psd1
```

当前比较重要的配置项：

- `OutputDirectory`
  - 日推输出目录，默认是 `daily-digests/`
- `DashboardDataDirectory`
  - 网页数据目录
- `WeeklyReportDirectory`
  - 周报目录
- `WeeklyReportTime`
  - 每周周报计划任务时间
- `KnowledgeTreeDirectory`
  - 知识树目录
- `ProjectDirectory`
  - 项目目录，当前用于周报和知识树参考

## 10. 知识树手工编辑数据

知识树手工覆盖文件在：

```text
.\knowledge-tree\manual-overrides.json
```

网页里的知识树保存，本质上就是修改这个文件，然后重建：

- `merged-tree.json`
- `auto-draft.json`

## 11. 常见问题

### 11.1 Firefox 提示无法连接 127.0.0.1:4173

原因通常是网页服务没跑。

处理：

1. 先关掉旧 `node` 进程
2. 再运行 `pwsh -File .\start_dashboard.ps1`
3. 刷新浏览器

### 11.2 周报页显示空白

原因通常是：

- 还没生成过周报
- `weekly-reports/index.json` 不存在

处理：

```powershell
pwsh -File .\generate_weekly_report.ps1
```

### 11.3 知识树修改后页面没更新

正常流程是保存后会自动写入 overrides 并重建知识树。

如果你怀疑数据没刷新，手动运行：

```powershell
pwsh -File .\build_knowledge_tree.ps1
```

然后刷新网页。

### 11.4 网页数据和 Markdown 不一致

手动重建：

```powershell
pwsh -File .\build_dashboard_data.ps1
pwsh -File .\build_knowledge_tree.ps1
pwsh -File .\generate_weekly_report.ps1
```

然后重新启动网页。

## 12. 推荐的一键刷新顺序

如果你今天刚改完 DDL、日推、知识树或周报，推荐按这个顺序：

```powershell
pwsh -File .\generate_research_digest.ps1
pwsh -File .\generate_weekly_report.ps1
pwsh -File .\build_knowledge_tree.ps1
pwsh -File .\start_dashboard.ps1
```

## 13. 当前已验证通过的测试

PowerShell：

- `.\tests\powershell\CommonModule.Tests.ps1`
- `.\tests\powershell\DashboardBuild.Tests.ps1`
- `.\tests\powershell\WeeklyReport.Tests.ps1`
- `.\tests\powershell\KnowledgeTree.Tests.ps1`

Node：

- `node --test .\tests\node\server.test.mjs`
